<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyCoffeeApp.Views.MyCoffeeDetailsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:MyCoffeeApp.Shared.Models;assembly=MyCoffeeApp.Shared"
    Title="{Binding Name}"
    x:DataType="model:Coffee"
    BackgroundColor="{AppThemeBinding 
                    Dark={StaticResource WindowBackgroundColorDark}, 
                    Light={StaticResource WindowBackgroundColor}}">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="{Binding Name, StringFormat='Name: {0}'}"
                   Style="{StaticResource LabelMedium}"/>
            <Label Text="{Binding Price, StringFormat='Price: {0:0,0 $}'}"
                   Style="{StaticResource LabelMedium}"/>
            <Label Text="{Binding Detail, StringFormat='Detail: {0}'}"
                   Style="{StaticResource LabelMedium}" />
            <Image HorizontalOptions="Center" Source="{Binding Image}" />
            <FlexLayout>
            <Button Clicked="Button_Clicked" Text="Add To Cart"
                    Style="{StaticResource ButtonOutline}"/>
            <Button Clicked="Button_Clicked_1" Text="Delete"
                    Style="{StaticResource ButtonOutline}"/>
            </FlexLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>